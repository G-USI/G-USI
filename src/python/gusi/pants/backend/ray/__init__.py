"""Ray Job Plugin for Pants.

This plugin provides a `ray_job` target that generates reproducible Ray job submissions
with automatically resolved transitive dependencies and pinned versions from the Pants lockfile.
"""

__version__ = "0.1.0"
